#include <assert.h>
#include <fstream>
#include "suffixArray.h"
#include <stack>
#include <algorithm>

using namespace suffixArray;


int main(int argc, const char * argv[])
{


// Main idea is to load a fold and contract BWT wt_int<> L & alphabet.
// For a 100 alphabet random items do rank (what about the position ? random or contant ? think..)
// Clock all the ranks together. Output the result
// Do the same for select (position ?)
// Repeat for all folds/datasets using parallel -> get an average time for 100ranks and 100selects


}